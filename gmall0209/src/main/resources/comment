定义侧输出流标签：
    1. 用户行为日志定义侧输出流类型为String，向kafka发送的数据；
    2. 注意用 {}

定义状态：
    1. 使用键控状态需要先按照key分流；
    2. valueState<String>：定义一个值类型的状态，每个键共享该值；
    3. 需要定义在RichFunction中，因为需要在Open方法中才能通过getRunContext获取状态，获取方法需要传入标签名和类型；
    4. 状态通过.value   .update来获取或者更新状态

数据库连接报错：
    有可能是异常没有抛出，导致返回类型不确定
    查询hbase数据的时候，需要同时指定schema